# Application Load Balancer default idle timeout is 60s. Preview "Host" runs a long synchronous
# build (npm install + vite) on POST /api/missions/:id/preview/start — ALB was closing the
# connection first → 502 / generic "internal error" in the browser.
option_settings:
  aws:elbv2:loadbalancer:
    IdleTimeout: "900"
