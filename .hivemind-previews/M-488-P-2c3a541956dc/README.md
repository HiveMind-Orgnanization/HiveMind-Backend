# Chess Game

## Overview
This project is a chess game with a React frontend and Node.js backend. It includes user authentication, game management, and a responsive UI.

## Installation

### Backend
1. Navigate to the `backend` directory.
2. Run `npm install` to install dependencies.
3. Run `npm start` to start the server.

### Frontend
1. Navigate to the `frontend` directory.
2. Run `npm install` to install dependencies.
3. Run `npm run dev` to start the development server.

## Features
- User authentication with JWT.
- Chess gameplay with an interactive board.
- Save and load game functionality.
- Chat feature for player communication.

## Tech Stack
- **Frontend**: React, Vite
- **Backend**: Node.js, Express
- **Database**: MongoDB (planned)
- **Authentication**: JWT

## Future Improvements
- Implement AI opponent.
- Add user registration and profile management.
- Enhance UI with better chess board and piece graphics.